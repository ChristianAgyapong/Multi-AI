# ─────────────────────────────────────────────────────────────────────────────
# Lean backend-only dependencies for the FastAPI service (deployed on Render).
# No Streamlit — the API does not need it.
# Install: pip install -r requirements-api.txt
# ─────────────────────────────────────────────────────────────────────────────

# API server
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.1

# RAG & embeddings
scikit-learn>=1.5.0
numpy>=1.26.0
pypdf>=4.3.0
python-docx>=1.1.0
google-genai>=0.2.2

# Vision
pillow>=10.4.0

# LLM providers + live facts
requests>=2.31.0
anthropic>=0.40.0
ddgs>=9.0.0

# Optional speech extras (comment in to enable TTS/STT on Render)
# gtts>=2.5.0
# faster-whisper>=1.0.0

